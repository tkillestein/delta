# Inputs

Helpers for coercing user inputs (NumPy arrays, astropy `CCDData`, FITS HDUs) into the
data/variance/mask layers the pipeline works with, and for synthesizing a variance map
from a detector noise model.

::: delta.as_layers

::: delta.synth_variance
